# 品牌投放H5 台账

> **追加式文件：只在各表末尾追加行，不改写、不删除历史**（唯一例外：「仍悬挂」的状态列可更新为闭环指向）。
> 新会话 / 接手同事先通读本文件，已确认口径不要再问一遍。引用口径时提示复核是否仍有效。

## 元信息

| 项 | 值 |
|---|---|
| 需求分析 | `docs/requirements/品牌投放H5_需求分析.md` + 设计图目录 |
| 技术方案 | 后端方案（王晨雨，2026-08-22 版） |
| 台账创建 | 2026-08-26（口径迁自 2026-08-22 产品书面回复 + 技术方案确认记录） |

## 阶段进度（断点续跑用）

| Step | 状态 | 产出物 | 更新日期 |
|---|---|---|---|
| 1 需求分析 | ✅ 已完成 | 需求分析文档已交付 | 2026-08-22 |
| 2-4 | ⬜ 未开始 | | |

## 已确认口径（追加）

| # | 确认日期 | 口径 | 确认人 / 来源 |
|---|---|---|---|
| 1 | 2026-08-22 | 从小程序返回 H5 **统一展示兜底页**，不区分拉起成功/失败、是否已支付；已支付后仍弹码是产品已接受的体感，不判缺陷 | 产品 胡晶晶（书面） |
| 2 | 2026-08-22 | 门店可售刷新沿用现有缓存逻辑；缓存窗口内下架→进详情走现有售罄/下架态，不新增兜底 | 产品 胡晶晶（书面） |
| 3 | 2026-08-22 | **反向红线**：不修改订单「来源渠道」字段（仍为微信小程序），投放归因靠旁路参数一路携带。开发改写该字段会不可逆污染存量统计——**必须设反向用例**；最硬约束：禁止静默切换到更远可售门店 | 产品 胡晶晶（书面） |
| 4 | 2026-08-22 | 「以门店实际售价为准」唯一感知手段是价格后「起」字，不加额外提示文案，不得据此提缺陷 | 产品 胡晶晶（书面） |
| 5 | 2026-08-22 | H5 关闭再打开 / 先点A后点B：兜底码与拉起参数始终指向用户**实际点击**的商品 | 产品 胡晶晶（书面） |
| 6 | 2026-08-22 | 半屏门店数 **3 家是上限非确数**，有几家展几家；一家都无→现有「附近暂无门店」且可打开附近门店列表 | 产品 胡晶晶（书面） |
| 7 | 2026-08-22 | 附近门店接口 `POST /v4/product-can-sell-near-shop/nearShops` 入参为「主数据商品ID」，接口内部查上层「销售商品」判各门店可售；productCode/productId 差异是两层模型体现非矛盾；出参 productId=销售商品ID（各店可不同），productCode 回显入参 | 后端 王晨雨 |
| 8 | 2026-08-22 | 降级：Feign 失败 productAvailable=false 即正常兜底。缓存 available 是状态值本身（可售/不可售都 HSET），不存在「只缓存可售态导致持续穿透」；TTL 沿用现有配置，Redis 兜底 10 分钟——用例用「HSET 把 time 改 now-1h」构造过期即可 | 后端 王晨雨 + 产品确认 |
| 9 | 2026-08-22 | registerSource 非必传，不传即不写入保持默认值；本期只新增 4 个枚举（28/29/30/31）、不改写入时机；非法值校验属既有注册逻辑 | 后端 王晨雨 |
| 10 | 2026-08-22 | 半屏返回条数「取前N家默认3」方案已写明非缺口；并发幂等「不涉及（纯查询）」不作本期功能项。后端本期范围仅两件：附近门店可售接口 + 注册来源枚举写入；订单归因走前端调 CDP、H5 数据前端直读 OSS、后台配置/券均不做 | 后端 王晨雨 |

## 分析纪律（本需求两次被纠正沉淀）

分析成熟系统的增量方案时，凡涉及**既有接口/既有数据结构/既有框架层**的行为，自行按既有逻辑推定或查代码确认，
不得包装成「需求缺口/待确认」推给用户去问开发。判定标准：**该问题是否由本期新增的代码/字段/结构引入**。

## 已判定不考虑（追加）

| # | 判定日期 | 项 | 理由 / 来源 |
|---|---|---|---|
| 1 | 2026-08-22 | 已支付后返回 H5 仍弹码的体感问题 | 产品已接受，不判缺陷 |
| 2 | 2026-08-22 | 「起」字之外的价格提示文案 | 产品明确不加 |

## 仍悬挂（未闭环）

| # | 提出日期 | 问题 | 状态 |
|---|---|---|---|
| 1 | 2026-08-22 | 归因窗口时长 / 首触还是末触 | 🔄 待确认 |
| 2 | 2026-08-22 | 注册来源能否成功写入（端到端验证） | 🔄 待确认 |
| 3 | 2026-08-22 | 2 个主推品全失效时的兜底表现 | 🔄 待确认 |
| 4 | 2026-08-22 | 埋点字段表冻结 | 🔄 待确认 |

## 变更日志（追加）

| 日期 | 操作人 | 事件 |
|---|---|---|
| 2026-08-26 | userzgc | 台账创建，口径自个人记忆迁入（记忆将于需求收口时按 memory-discipline 退役） |
